# ── Core (needed for paper mode) ─────────────────────────────────────────────
requests
pandas
numpy
scikit-learn
streamlit
plotly
python-dotenv
aiohttp
asyncio-throttle
sqlalchemy
apscheduler
colorlog
websockets
websocket-client
ccxt
httpx

# ── LIVE trading only (lazy-imported, install separately if going live) ───────
# Keep py-clob-client CURRENT — the CLOB exchange rejects orders signed by an
# outdated client ("invalid order version, please use the latest clob-client").
# pip3 install -U py-clob-client web3
