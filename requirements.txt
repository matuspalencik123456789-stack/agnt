# ── Core (needed for paper mode) ─────────────────────────────────────────────
requests
pandas
numpy
scikit-learn
streamlit
plotly
python-dotenv
aiohttp
asyncio-throttle
sqlalchemy
apscheduler
colorlog
websockets
websocket-client
ccxt
httpx

# ── LIVE trading only (lazy-imported, install separately if going live) ───────
# CLOB V2 (2026) requires the V2 SDK — the legacy py-clob-client (V1) signs an
# outdated EIP-712 version the server rejects ("invalid order version"). Use:
# pip install py-clob-client-v2 web3
